# Contributing

DevForge Toolkit is developed as the automation layer for DevForge HQ.

## Principles

- Repository data stays outside the toolkit.
- Everything is modelled as an entity.
- Commands should work through `DevForgeRepository` where possible.
- Public commands should use approved PowerShell verbs.
- Keep functions small and focused.
